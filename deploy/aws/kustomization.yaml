apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Deliberately manual base: optional-profile-secrets.yaml is an opt-in overlay
# and must not be included merely because it is a sibling manifest.
resources:
  - secret-store-and-credentials.yaml
