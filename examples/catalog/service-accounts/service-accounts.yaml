apiVersion: platform.example.org/v1beta1
kind: GrafanaServiceAccounts
metadata:
  name: replacewithunique01
  namespace: grafana-vending
spec:
  # This must name a ready GrafanaCloudStackRequest in this namespace.
  stackRef:
    name: replacewithunique01
  # The profile, role, permission set, and token lifetime are owned by the
  # enforced Composition. The request only selects an approved profile.
  profile: ci
  accounts:
    - name: ci-runner
