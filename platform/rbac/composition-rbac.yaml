apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: crossplane-compose-grafana-vending-secrets
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    rbac.crossplane.io/aggregate-to-crossplane: "true"
# Deliberately manual support-resource allowlist: provider controllers obtain
# managed-resource access from their CRDs; rendered GVKs do not define this role.
rules:
  - apiGroups:
      - external-secrets.io
    resources:
      - pushsecrets
      - pushsecrets/status
      - externalsecrets
      - externalsecrets/status
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
